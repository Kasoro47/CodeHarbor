replicaCount: 2
image:
  repository: nginx
  tag: "1.16.1"
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  annotations: 
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: instance-ip.nip.io
      paths:
        - /
